import { StationTransferPipe } from './station-transfer.pipe';

describe('StationTransferPipe', () => {
  it('create an instance', () => {
    const pipe = new StationTransferPipe();
    expect(pipe).toBeTruthy();
  });
});
